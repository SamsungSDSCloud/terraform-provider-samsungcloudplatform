# SCP Terraform Provider

## Directory structure

 - `client` : Wrapper APIClient library
 - `common` : Commonly used constants & functions
 - `service` : Actual resource/data-source implementations
 - `provider.go` : SCP terraform provider
